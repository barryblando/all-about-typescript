{
  "scripts": {
    "start": "concurrently --kill-others \"yarn start:parcel\" \"yarn start:db\"",
    "start:db": "json-server -w db.json",
    "start:parcel": "parcel index.html"
  },
"devDependencies": {
    "concurrently": "^4.1.2",
    "parcel-bundler": "^1.12.3",
    "typescript": "^3.6.3"
  },
  "dependencies": {
    "axios": "^0.19.0"
  }
}
